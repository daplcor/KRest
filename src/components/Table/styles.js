import { grey } from "@mui/material/colors"

export const styles = {
    link: {
        textDecoration: "none"
    },
    table: { minWidth: 450, backgroundColor: grey[200], border: 1, borderColor: grey[200] },
    tableBody: { backgroundColor: '#fff' },
    tableContainer: {
        borderRadius: "6px"
    }
}
